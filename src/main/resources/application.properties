# configure your datasource
quarkus.datasource.db-kind = postgresql
quarkus.datasource.username = mansour
quarkus.datasource.password = Access/fedora/6
quarkus.datasource.jdbc.url = jdbc:postgresql://localhost:5432/moood

# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.database.generation = drop-and-create

# docker run --name postgres -e POSTGRES_USER=mansour -e POSTGRES_PASSWORD=psql -e POSTGRE_DB=mansour -p 5432:5432 -d postgres